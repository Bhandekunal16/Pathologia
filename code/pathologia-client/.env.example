# Optional override for the backend API base URL (no trailing slash).
# If unset, the app auto-detects:
#   - Local (localhost) -> http://localhost:3000
#   - Vercel (*.vercel.app) -> https://pathologia-server.vercel.app
# VITE_API_BASE_URL=
